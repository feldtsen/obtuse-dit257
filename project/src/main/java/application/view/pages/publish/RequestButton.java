package application.view.pages.publish;


import javafx.scene.control.Button;

// Button for marking a post as a request
public class RequestButton extends Button {
    public RequestButton() {
        this.setText("Request");
    }
}
